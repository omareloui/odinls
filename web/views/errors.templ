package views

import "strconv"

templ ErrorPage(message string, status int) {
	@baseLayout("Error | Odin Leather Store") {
		@container() {
			<h1 class="text-2xl mb-2">Error</h1>
			<p>Status: { strconv.Itoa(status) }</p>
			<p>Message: { message }</p>
		}
	}
}
